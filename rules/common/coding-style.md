# Coding Style

- Python: type hints, dataclasses, async где нужно
- Логи через logging, не print
- Конфиги через .env / os.getenv
- Docker для сервисов где возможно
- Git: feat/fix/chore префиксы в коммитах
- Ветки: dev → main через PR
